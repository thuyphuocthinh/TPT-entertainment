nav.navbar.bg-dark
    #navbar
        a.navbar-brand(href='#') TPT-entertainment
        .navbar-right 
            a(href=`${prefixAdmin}/accounts`)  #{user.email} - #{role.title}
            a(href=`${prefixAdmin}/auth/logout`).btn.btn-secondary Đăng xuất